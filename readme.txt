Building
--------

* Run mvn clean install
* To create the zip distribution, run mvn clean install -Drelease - the resulting zip is located in dist/target/seam-config-${version}.zip 
* The readme.txt placed in the distribution is not this one, see dist/src/main/assembly/readme.txt 
